/*
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main() {

	// 삼항 조건 연산자
	// 3개의 항을 이용하여 참, 거짓에 따라 결과값을 다르게 표현할 때 사용
	// 다만, if 또는 if ~ else 를 이용하면 더 많은 결과값을 얻을 수 있기때문에
	// 간단한 조건인 경우를 제외하고는 잘 안쓴다.

	// 사용법
	// 조건 ? 결과1 : 결과2;

	int num, absolute;

	printf("정수 입력 : ");
	scanf("%d", &num);

	absolute = num > 0 ? num : num * -1;
	printf("절대값 : %d\n", absolute);


	return 0;
}
*/