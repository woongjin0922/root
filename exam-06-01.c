/*
#include<stdio.h>

int main() {

	// 논리연산 : 참과 거짓을 가지고 연산하여 결과가 참 또는 거짓으로 나오게 하는 계산
	// 논리연산자 : 논리 연산에 사용되는 기호
	// && : AND(주어진 조건이 모두 참인경우 결과가 참으로 출력하는 연산자)
	// || : OR(주어진 조건 어느 하나라도 참인경우 결과가 참으로 출력하는 연산자)
	// ! : NOT(연산의 결과를 반대로 출력하는 연산자 예) 참 -> 거짓)

	int num1 = 20, num2 = 10;
	int result;

	result = 1 && 1;
	printf("result1 : %d\n", result);

	result = num1 == 20 && num2 == 10;
	printf("result2 : %d\n", result);

	result = num1 > num2 && num1 == num2;
	printf("result3 : %d\n", result);

	return 0;


}
*/

